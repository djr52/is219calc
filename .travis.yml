language: node_js
node_js:
  - '8'
branches:
  only:
    - master
cache:
  directories:
    - node_modules
before_install:
  - npm update
  - chmod 0555 ./node_modules/.bin/jest
  - chmod +x ./tests/coverage/lcov.info
install:
  - npm install
script:
  - npm test
  - npm run coveralls